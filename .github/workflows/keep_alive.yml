name: AirSentinel - Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *' # Toutes les 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up Render API
        run: curl -I https://airsentinel2.onrender.com/health
